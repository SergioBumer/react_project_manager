const retrieveUsers = (req, res) => {
    res.json({msg: "from api/users"})
};

const createUser = (req, res) => {
    res.json({msg: "from api/users"})
};

export {
    retrieveUsers,
    createUser
} 